class PagesController < ApplicationController
  def home
    # essa funcao fica vazia pois a classe vai procurar nos views o metodo para renderizar. O padrão é renderizar o que estiver no view.
  end
end
